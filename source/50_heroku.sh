#!/bin/bash

export HEROKU_ORGANIZATION=financial-times

# Heroku CLI autocomplete support.
[[ -f "$HOME/Library/Caches/heroku/autocomplete/bash_setup" ]] && source "$HOME/Library/Caches/heroku/autocomplete/bash_setup";
